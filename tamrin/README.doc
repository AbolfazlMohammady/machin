خب برای اجرای پروژه به صورت زیر عمل کنید
ساخت محیط مجازی با دستور:
python -m venv venv
بعد ساخته شدن محیط مجازی باید اونو تو محیط توسعه فعال کنید
./venv/Script/Activeat
بعد از فعال سازی شروع به نصب کنید
pip install -r requirements.txt

بعد از  نصب وابستگی ها  باید به  فایل های جوپیتر بگی کدوم محیط مجازیه 
=========


خب ما ابتدا یک فایلpreprocess.ipynb درست کردیم
این فایل شامل مراحل پیش پردازش دیتای ما هست 
=========

یک پوشه با نام الگوریتم ساخته شده که تو اونجا سه تا الگوریتم ر. برسی کردیم 
اولین الگوریتمی که برسی کردی الگوریتم Kmeans

طبق برسی الگوریتم ها متوجه شدیم که الگوریتم DBSCAN عملکرد بهتری داشت
البته با دستکاری eps 
که مقدار 2.7 بهش دادم ومقدار96.25660902498198 برگرداند که بیشترین مقدار بود